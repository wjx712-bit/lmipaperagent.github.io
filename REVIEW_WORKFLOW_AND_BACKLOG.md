# 평가 운영 기준과 후속 개선안

기준일: 2026-09-11. 연구실 멤버들이 주제를 나눠 평가하는 현재 작업을 지원합니다.

## 이번 적용 범위

- 모바일 헤더, 배너, 통계, 필터, 논문 상세 및 저장 버튼의 가로 넘침 방지.
- 목록은 한국어 초록 미리보기 2줄 우선 표시. 한국어가 없으면 영어를 표시하고, 상세창에는 기존 영문과 한글 전문을 유지.
- 비로그인 상태의 후보 수와 평가 진행률은 `로그인 후 확인`, 미승인 계정은 `승인 후 확인`으로 표시.
- 일반 멤버는 `내 평가`, 관리자는 `내 평가 / 연구실 전체` 집계 범위를 선택.
- 현재 검색어, 주제, 저널, 점수 필터, 탭에 해당하는 논문 중 자신의 미평가 논문을 연속 검토.
- 이전/다음 논문, 평가 저장, 저장 후 다음, 마지막 논문의 저장 후 완료 제공. 저장 후 다음 논문은 초록부터 열립니다.
- 검토 시작 시 순서를 고정. 저장으로 후보 분류나 정렬이 바뀌어도 다음 논문이 누락되지 않음.
- 이동/닫기 전 미저장 변경 확인. 저장 실패 시 현재 논문, 점수, 노트 유지.
- 승인 멤버에게 다른 멤버의 평가 완료 여부와 최초 평가 경로만 공유. 다른 사람의 점수, 노트, 신원은 DB의 공유 조회 응답에도 포함하지 않음.
- 전체 평가 진행률은 현재 공개된 고유 논문 중 한 명 이상 평가한 비율. 같은 논문의 복수 평가는 한 편으로 집계.
- 작업 주제 경유 진행률은 해당 경로로 평가가 실제 기록된 고유 논문 비율. 기존 경로 미기록 평가는 전체 진행률에는 포함하지만 특정 주제 경유 완료로 추정하지 않음.
- 공유 현황은 저장 직후, 화면 복귀, 전경 60초 간격 및 수동 새로고침으로 갱신. 실패하면 미평가로 오인하지 않도록 공유 필터 결과를 차단.

## 평가의 의미

사용자 확인: 점수는 담당 주제가 아니라 **연구실 전체와의 관련성**을 평가합니다. 주제 배정은 업무 분담이며, 같은 이유로 낮게 평가한 기록을 담당 주제만의 판단으로 임의 해석하지 않습니다. 2026-09-10 미분류 보강은 주제 목록만 추가했으며 기존 원점수·노트와 이 평가 기준을 변경하지 않았습니다.

이미 진행 중인 라벨링의 일관성을 위해 기존 1~5점 의미와 저장 형식을 유지합니다.

| 값 | 기존 의미 | 처리 |
| --- | --- | --- |
| 1 | 제외 | 저장된 명시적 평가. 미평가와 다름 |
| 2 | 보관 | 낮은 우선순위의 평가 자료로 보존 |
| 3 | 검토 | 검토 후보 |
| 4 | 우선 검토 | 검토 후보 |
| 5 | 필독 | 필독 후보 |
| 미평가 | 아직 판단하지 않음 | 학습 시 1점 또는 0점으로 취급하지 않음 |

- `내 평가`: 해당 멤버가 부여한 연구 관련성 점수와 개인 리뷰 노트. 논문의 과학적 품질을 객관적으로 확정하는 점수가 아닙니다.
- `연구실 최고점`: 관리자용 후보 탐색 집계입니다. 적어도 한 명이 5점을 주면 필독 후보, 최고점이 3~4점이면 검토 후보입니다. 연구실의 합의 또는 확정 학습 정답을 뜻하지 않습니다.
- 주제별 담당자가 다르므로 미평가를 낮은 점수로 넣거나, 서로 다른 주제를 평가한 멤버의 점수를 단순 평균으로 합치지 않습니다.
- `전문가 추천`: 교수님/박사님 추천 정보는 독립적으로 유지합니다. 멤버 원점수를 자동으로 대체하지 않습니다.
- DB는 사용자와 논문당 점수/노트 1개를 저장합니다. `review_topic`은 최초 평가 경로이며 다른 경로로 재방문하거나 평가를 수정해도 바뀌지 않습니다. 기존 NULL은 경로 미기록, `__general__`은 명시적인 전체 목록 경로입니다. 동일 인물의 재방문을 독립 평가로 중복 집계하지 않습니다.
- 평가 도중 기준을 재정의하면 기존 점수와 비교가 어려워집니다. 더 구체적인 기준은 다음 평가 회차에 버전을 붙여 적용하는 방안을 권합니다.

## 주제별 작업 흐름

1. 담당 주제를 왼쪽 연구 주제 필터에서 선택합니다. 별도 작업 주제 선택 없이 하나는 해당 경로, 미선택은 전체 목록 경로로 자동 지정됩니다. 여러 주제를 선택한 경우에만 검토 시작 시 경로를 한 번 선택하고, 주제 필터를 바꾸기 전까지 재사용합니다. 특정 작업 주제는 기존 검색/저널/주제 필터와 교집합으로 적용됩니다. 기존 평가의 최초 경로는 바뀌지 않습니다.
2. `미평가 연속 검토`는 현재 목록 안에서 본인이 평가하지 않은 논문을 엽니다. 타인이 평가해도 자동 제외하지 않습니다. 선택형 `현재 작업 주제에서 미평가만`은 해당 경로의 평가가 없는 논문만 표시하며, 타 주제/미기록 경유 평가만 있는 논문은 남습니다. 관리자는 필요하면 `내 평가` 범위로 전환합니다.
3. 초록 확인 후 `내 평가`에서 점수와 노트를 기록합니다. 저장 후 다음 논문으로 이동할 수 있습니다.
4. `다음 논문`만 눌러 건너뛴 논문은 미평가로 남습니다. 종료 후 미평가 연속 검토를 다시 시작하면 포함됩니다.

## 이번에 적용하지 않은 개선안

2026-09-10 추가 반영: 아래 항목 중 주제별 평가 추합, 전체 페이지 조회, 주제별 진행률, 불일치 후보, 분류 품질 점검, 저널·주제 분포는 관리자 전용으로 구현했습니다. 학습이나 자동 분류 교정은 수행하지 않았습니다.

| 우선순위 | 항목 | 제안 및 선행 결정 |
| --- | --- | --- |
| 높음 | 평가 기준 버전 관리 | 기존 라벨을 그대로 보존하고 향후 기준은 새 버전으로 구분. 점수 경계 사례를 팀이 함께 합의 |
| 높음 | 수집 운영 상태 | 마지막 성공/실패, 저널별 수집 수, 초록/번역 대기 수 표시. 2026-09-10 확인 시 평가를 위해 일요일 자동 수집이 일시정지되어 있으며, 재개는 별도 결정 |
| 중간 | 초기 데이터 크기 | 목록 메타데이터와 상세 초록을 분리하고 상세 열람 시 초록 요청. 기존 DOI/논문 ID는 유지 |
| 중간 | 담당자 배정·평가 회차 | 최초 평가 경로 기록은 구현. 담당자별 분배/예약/잠금, 배정 대비 완료율, 공통 논문 독립 평가 샘플 배정은 별도 운영 결정 필요 |
| 중간 | 분류 교정 | 보강 초록 기준 재분류, 일반 대사/간 특이 주제 분리, 동의어 확장. 기존 평가 세트는 자동 변경하지 않고 근거와 버전을 남긴 뒤 검토 |
| 중간 | 주간 화면 | 평가용 전체 목록과 주간 업데이트 기본 진입 화면의 분리 여부 결정 |

## 관리자 주제별 현황

1. 관리자 로그인 후 계정 메뉴의 `평가 관리`에서 `저널·주제 분포`, `주제별 평가`, `평가 불일치 후보`, `분류 품질 보고서`를 확인합니다. 일반 멤버에게 이 메뉴와 타인 점수/노트/신원은 제공하지 않으며 익명 완료 현황만 공유합니다.
2. 주제·저널 필터는 현재 논문 분류를 기준으로 적용합니다. 다중 주제 논문은 각 주제에 포함되며 전체 합계는 고유 논문 수입니다. 미분류도 별도로 표시합니다.
3. 주제별 진행률은 해당 범위에서 평가 1건 이상이 있는 고유 논문 수 / 해당 범위 논문 수입니다. 담당자 배정 대비 완료율이 아닙니다. 평가자 필터를 선택하면 그 평가자의 기록만 집계합니다.
4. 기존 점수 1점도 평가 완료입니다. 미평가는 낮은 점수로 바꾸지 않습니다. 점수 분포는 개인별 원점수 건수이며 최고점/평균으로 축약하지 않습니다.
5. 불일치 후보는 서로 다른 평가자 2명 이상, 최고점-최저점 2점 이상입니다. 1~2점과 4~5점이 공존하면 우선 확인입니다. 현재 논문 주제로 묶을 뿐, 같은 담당 주제로 평가한 사실이나 합의 실패를 확정하지 않습니다. 불일치 비교는 평가자 필터와 관계없이 전체 평가자를 사용합니다.
6. CSV/JSON은 현재 범위의 사용자-논문별 최신 원점수, 노트, 평가자, 시각, DOI, 현재 논문 주제 및 최초 평가 경로를 포함합니다. 새 경로는 `review_topic_status=recorded`, 기존 미기록은 `review_topic=null`/`not_recorded`입니다. `rubric_version`은 여전히 미기록입니다. CSV는 위험한 텍스트 앞에 작은따옴표를 붙여 수식 실행을 막고 JSON은 원문을 보존합니다.
7. 원점수 중 현재 논문 목록에 없는 기록, 무효 점수, 중복은 집계에서 제외한 수를 표시합니다. DB 기록은 삭제하지 않습니다. 차단된 멤버의 과거 평가도 기록으로 유지됩니다.
8. 개인 평가·리뷰 노트는 기존 Supabase RLS로 본인/관리자만 접근합니다. 관리자 화면은 조회 전후 `is_lmi_admin`을 확인하며, 조회 실패·권한 회수 시 부분 집계와 내보내기를 막습니다. 계정 변경 시 이전 관리자 화면을 폐기합니다. 키 기반 커서 조회는 앞쪽 기록 삭제로 뒤쪽 기록이 건너뛰어지는 것을 방지합니다. 다만 여러 DB 요청을 합치므로 단일 시점 트랜잭션 스냅샷은 아닙니다. 최종 학습 데이터 확정 시 평가 입력 일시정지 또는 서버 스냅샷이 필요합니다.
9. 보고서와 후보 CSV는 브라우저에서 로컬로 내려받습니다. 개인별 정보가 포함된 관리자 보고서를 공개 GitHub 저장소에 올리지 않습니다. 공개 논문 데이터에서 계산되는 분포/품질 규칙은 비밀 데이터가 아니지만 화면은 관리자 전용입니다.
10. 분류 품질 수치는 용어 기반 확인 후보입니다. 링크 접속, 원문 전체, 번역 정확성, 정밀도/재현율은 검증하지 않았습니다. 초록 보강 후 기존 분류와 차이는 `python -m paper_agent.audit_classification_alignment --summary-only`로 읽기 전용 재현이 가능합니다.

## 향후 학습 시 주의할 데이터 해석

- 멤버별 원점수는 보존하고 최고점 집계 값만 학습 자료로 사용하지 않습니다.
- 논문 DOI별로 학습/검증/테스트를 분리해 같은 논문의 다른 멤버 평가나 번역이 검증 자료로 섞이지 않도록 합니다.
- 주제 필터에서 선택했다는 사실과 실제 해당 주제로 평가했다는 사실은 구분해야 합니다. 기존 기록의 담당 주제는 배정표로 확인하고, 확실하지 않은 경우 추정값임을 표시합니다.
- 사용자별 리뷰 노트의 접근 권한은 본인과 관리자에게만 유지합니다. 내보내기는 관리자 전용으로 설계합니다.

## 검증 재실행

`pnpm build`로 프런트엔드를 빌드합니다. 로컬 Vite 서버를 실행한 뒤 Playwright와 Chrome이 설치된 환경에서 `node tests/ui-review.mjs`를 실행합니다.

- 기본 테스트 주소: `http://127.0.0.1:8793/` (`LMI_UI_URL`로 변경 가능).
- Playwright가 외부 런타임에 있으면 `LMI_NODE_MODULES`에 해당 `node_modules` 경로를 지정합니다.
- 테스트는 로컬 주소만 허용하며, 인증/저장 모듈을 테스트 더블로 대체합니다. 실제 Supabase에 평가를 저장하지 않습니다.
- 스크린샷은 Git에서 제외되는 `.cache/ui-review/`에 생성됩니다.
- `pnpm test:coordination`은 진행률/경로/필터 로직과 PGlite PostgreSQL 엔진에서 실제 SQL 마이그레이션, 원본 보존, RLS, 권한 거부 및 응답 제한을 검증합니다. 운영 DB에는 테스트 평가를 작성하지 않습니다.

관리자 단위 검사는 `node --test tests/admin-analytics.test.mjs tests/admin-data.test.mjs tests/classification-audit.test.mjs`, 관리자 UI 검사는 동일한 로컬 서버에서 `node tests/ui-admin.mjs`로 실행합니다. 관리자 테스트의 1,101건 평가는 테스트 더블이며 실제 연구실 평가 수가 아닙니다. 실제 Supabase에 연결하거나 기록하지 않습니다.
