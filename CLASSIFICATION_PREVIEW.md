# 새 분류 미리보기 운영 원칙

## 변경 금지 경계

연구실 멤버는 이미 기존 논문 주제로 작업을 배정받았습니다. 사용자 요청에 따라 이번 기능은 **관리자 확인용의 별도 초안**입니다.

- `public/data/papers.json`의 `topics`, 논문 ID, 초록, 기존 관련성 점수는 변경하지 않습니다.
- `config/lab_profile.yml`, `config/relevance_rubric.yml`, 누적 카탈로그와 수집기는 그대로입니다.
- 일반 멤버의 주제 목록, 평가 대기열, 진행률, 개인별 점수/노트, 연구실 집계에 초안을 연결하지 않습니다.
- Supabase 테이블이나 스키마에 새 분류를 저장하지 않습니다. 기존 평가를 이동·복제·재분류하지 않습니다.
- 적용/승인/덮어쓰기 버튼이 없습니다. 관리자가 초안을 확인했다는 사실도 운영 전환 승인으로 간주하지 않습니다.

## 확인 위치

관리자 로그인 후 계정 메뉴 → `평가 관리` → `새 분류 미리보기`.

`기존 할당 기준`에서 현재 주제를 선택하면 기존 멤버 배정 범위만 대상으로 비교할 수 있습니다. 새 핵심 후보, 저널, 변경 여부, 검토 필요, 제목/DOI로도 필터링할 수 있습니다.

기존 태그에는 핵심/연관 구분이 없으므로 새 핵심 후보 수와 단순히 같은 의미로 비교하지 않습니다. 화면의 ‘핵심에서 빠짐’은 미리보기에서의 차이이며 운영 데이터의 실제 삭제가 아닙니다.

## 실험 엔진

- 입력은 제목과 최종 영문 초록입니다. 기존 태그는 비교용으로만 사용합니다. 기존 관련성 점수, 개인 평가, 한국어 번역, 저널 이름을 분류 근거로 사용하지 않습니다.
- 간 특이 용어와 일반 대사 용어를 분리합니다. 일반 비만/당뇨/지질대사 또는 fibrosis 단독으로 간 핵심 후보를 만들지 않습니다.
- 지방조직·지방세포의 직접 표현을 사용합니다. 지방량·BAT 약어·미토콘드리아만으로 지방 핵심 후보를 확정하지 않습니다.
- 실제 연구 서술 신호, 제목의 명시 표현, 배경/추측/부정 표현을 구분하는 보수적 규칙입니다. 사람처럼 문맥을 이해하는 확정 판정이나 학습된 AI 모델이 아닙니다.
- 단일세포 RNA, 단일핵 RNA, ATAC, 공간 전사체 등의 표현 변형을 탐지하고 구분합니다. 수행 근거가 불명확한 방법은 연관·검토 후보로 남깁니다.
- 치료 표적이라는 가능성 언급만으로 치료 실험을 확정하지 않습니다.
- 근거는 원래 제목/초록의 문장과 UTF-16 시작/끝 위치입니다. 판단 신호는 절 단위로 확인하며 근거 표시는 원문 문장 전체를 보존합니다. 없는 문장을 생성하지 않습니다.
- 명시적인 근거로 분류된 경우도 연구실 적합성이나 분류 정확성이 검증된 것은 아닙니다. 부정 표현과 절 경계를 보수적으로 처리하므로 누락될 수 있습니다.
- 외부 API 호출이나 유료 LLM 분석은 하지 않습니다. 애매한 경우는 관리자 검토 대상으로 남깁니다.

## 독립 결과 파일

CSV와 JSON은 현재 필터 범위의 초안만 다운로드합니다. 다음 정보가 포함됩니다.

- 기존 주제, 새 핵심·연관 후보, 추가/빠진 핵심 후보, 방법 후보.
- 근거 구절·위치·판정 사유와 검토 필요 여부.
- 초안 버전, 입력 SHA-256, 생성 시각, `applied: false`.
- 개인별 점수·노트·이메일은 포함하지 않습니다.
- CSV의 `preview_decision`, `preview_note`는 초안 검토용 빈 칸입니다. 기존 논문 평가 점수와 별개이며 자동 업로드/학습 기능은 없습니다.

입력 해시는 ID 순으로 정렬한 공개 분류 입력을 기준으로 계산합니다. 영문 초록 또는 기존 주제가 바뀌면 해시가 달라집니다. 한국어 번역과 개인 평가의 변화는 분류 입력 해시를 바꾸지 않습니다.

공개 논문과 규칙을 이용해 브라우저에서 계산하므로 화면은 관리자 전용이어도 알고리즘 자체가 비밀은 아닙니다. 결과 JSON을 공개 데이터 경로에 게시하지 않습니다.

## 오프라인 재현

```powershell
pnpm test:preview
node scripts/export_classification_preview.mjs
```

기본 출력은 Git에서 제외되는 `.cache/classification-preview/preview.json`과 `preview.csv`입니다. 이미 존재하는 파일은 덮어쓰지 않습니다. 새 실험은 `--output-dir`로 별도 폴더를 지정합니다. `public`, `dist`, `data`, `config`, `src` 등 운영·배포 폴더로의 출력은 차단합니다.

향후 전환은 별도 요청으로 진행합니다. 담당자 재배정 문제를 먼저 해결하고, 원본 분류·평가를 보존한 버전 전환 설계가 선행되어야 합니다.

## 2026-09-10 초안 관찰

`classification-preview-1.1.0-draft`를 현재 2,797편에 적용한 별도 실험입니다. 기존 분류와 핵심 후보가 다른 논문은 1,295편, 핵심 후보를 설정하지 못한 논문은 621편입니다. 운영 변경 건수는 0건입니다.

| 주제 | 기존 태그 | 새 핵심 후보 | 새 연관 후보 |
| --- | ---: | ---: | ---: |
| Liver metabolism / MASLD | 868 | 429 | 198 |
| Adipose tissue / adipocyte biology | 540 | 353 | 155 |
| Inflammation / immune regulation | 755 | 964 | 537 |
| Aging / senescence | 259 | 261 | 157 |
| Single-cell / spatial / atlas | 77 | 274 | 177 |
| Cell targeting / therapy | 40 | 130 | 1,665 |
| General metabolism / obesity / insulin resistance | 0 | 406 | 902 |

주제 간에는 중복이 있습니다. 핵심/연관은 같은 주제 안에서만 상호 배타적입니다. 기존 태그와 새 핵심 후보의 의미가 달라 정확도 향상이나 관련 논문의 감소를 뜻하지 않습니다. 예를 들어 기존 지방조직 담당 범위는 계속 540편입니다.

### 확인할 한계

- `검토 필요`는 2,685편으로 넓습니다. 분류 차이뿐 아니라 배경·추측·부정 표현이나 연관 근거만 있어도 표시합니다. 검증된 오류 건수나 검토 우선순위 점수가 아닙니다.
- 치료/표적 용어는 일반 논문에서도 흔하므로 연관 후보 1,665편에는 잡음이 많을 수 있습니다. 세포 표적 전달과 일반 약물 개입의 구분도 사람의 검토가 필요합니다.
- 연구를 실제로 수행한 문장도 간접적인 문체나 혼합 문장 때문에 연관으로 낮아질 수 있습니다. 핵심 후보가 없는 621편을 제외하거나 삭제하면 안 됩니다.
- 제목에 명시된 용어를 핵심 후보로 인정하지만, 연구의 가장 중요한 주제인지까지 검증하지는 않습니다.
- 기존 주제별로 달라진 사례와 유지된 사례를 함께 검토한 뒤 규칙을 수정하는 것이 적절합니다. 멤버의 1~5점 평가를 새 분류의 정답으로 자동 해석하지 않습니다.

입력 해시: `69125defd920af18112568e93836a9a1e0f63efc3c3dd212d7ddfebb58d7230b`.
